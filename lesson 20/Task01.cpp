#include <iostream>

using namespace std;

unsigned long long fibonachi(int index);

int main(){
	int index;


	do {
		cout << "Plsss input index: ";
		cin >> index;

	} while (index <= 0);

	unsigned long long number = fibonachi(index);

	cout << "Fibaonachi number with index " << " = " 

	system("pause");
	return 0;
}

unsigned long long fibonachi(int index) {
	int tries;
	int value = 0;


	for (int i = 0; i < tries; i++) {

		tries--;
	}
	return 0;
}